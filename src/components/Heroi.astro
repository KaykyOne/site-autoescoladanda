---
import { Image } from "astro:assets";
---

<section class="relative w-full min-h-screen overflow-hidden flex items-center pt-10">

    <!-- Background -->
    <Image
        src={"/bg-hero.jpg"}
        alt="Imagem Autoescola"
        class="absolute inset-0 w-full h-full object-cover scale-105"
        loading="eager"
        height={400}
        width={400}
    />

    <div class="absolute inset-0 bg-gradient-to-r from-black/90 via-black/80 to-primary-dark/50"></div>

    <!-- Conteúdo -->
    <div class="relative z-10 max-w-7xl mx-auto w-full px-6 lg:px-12 py-24 grid lg:grid-cols-2 gap-12 items-center">
        <!-- Lado esquerdo -->
        <div class="text-secondary flex flex-col">
            <!-- Badge -->
            <div class="bg-white backdrop-blur-md rounded-full px-5 py-2 w-fit mb-6">
                <p class="text-sm font-medium flex items-center gap-2 text-primary">
                    <span class="material-symbols-outlined text-base">airport_shuttle</span>
                    Sua jornada para a CNH começa aqui
                </p>
            </div>

            <!-- Headline -->
            <h1 class="text-4xl sm:text-5xl lg:text-6xl font-light leading-tight tracking-tight">
                Autoescola
            </h1>

            <h2 class="text-5xl sm:text-6xl lg:text-7xl font-extrabold text-white leading-none mt-2">
                DANDA
            </h2>

            <div class="h-1 w-24 bg-primary mt-4 rounded-full"></div>

            <!-- Descrição -->
            <p class="mt-8 text-white/80 text-lg max-w-xl leading-relaxed">
                Mais de 20 anos de experiência formando condutores com segurança, método prático e alta taxa de aprovação.
                Categorias A e B com preços acessíveis e parcelamento facilitado.
            </p>

            <!-- CTAs -->
            <div class="mt-10 flex flex-col sm:flex-row gap-4">

                <a
                    href="#contato"
                    class="bg-secondary hover:bg-secondary/90 text-white font-semibold rounded-full px-8 py-4 shadow-2xl shadow-primary/40 transition-all duration-300 hover:scale-105 flex items-center justify-center gap-2"
                >
                    <span class="material-symbols-outlined text-xl">calendar_month</span>
                    Agende sua aula
                </a>

                <a
                    href="/planos"
                    class="bg-white/10 backdrop-blur-md border border-white/20 text-white rounded-full px-8 py-4 font-medium hover:bg-white/20 transition-all duration-300 flex items-center justify-center gap-2"
                >
                    <span class="material-symbols-outlined text-xl">calculate</span>
                    Simular plano
                </a>

            </div>

        </div>

        <!-- Lado direito (Estatísticas Flutuantes) -->
        <div class="hidden lg:flex relative items-center justify-center">

            <!-- Card 1 -->
            <div class="absolute top-10 left-10 bg-white/10 backdrop-blur-xl border border-white/20 rounded-2xl p-8 text-white shadow-2xl w-64">
                <div class="flex flex-col items-center gap-4">
                    <span class="material-symbols-outlined text-6xl text-primary">
                        directions_car
                    </span>
                    <p class="text-3xl font-bold">95    %</p>
                    <p class="text-sm opacity-80">De Aprovação</p>
                </div>
            </div>

            <!-- Card 2 -->
            <div class="absolute bottom-10 right-10 bg-white/10 backdrop-blur-xl border border-white/20 rounded-2xl p-8 text-white shadow-2xl w-64">
                <div class="flex flex-col items-center gap-4">
                    <span class="material-symbols-outlined text-6xl text-amber-400">
                        payments
                    </span>
                    <p class="text-xl font-semibold text-center">
                        Parcelamento facilitado
                    </p>
                </div>
            </div>

        </div>

    </div>

    <!-- Linha inferior -->
    <div class="absolute bottom-0 left-0 w-full h-1 bg-primary"></div>

</section>